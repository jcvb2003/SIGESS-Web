# Task Completion Checklist

After any code change in Web:

1. `npx tsc --noEmit` — zero errors required
2. `npm run lint` — zero warnings/errors
3. No test runner exists yet — manual smoke test in browser

When test infrastructure is added, update this memory with the test command.
